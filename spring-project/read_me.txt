The folder nlp-stats-app-2 has the source project alone.
The export-rk.zip is a compressed folder that has the source project and the libraries.
Download and Extract export-rk.zip folder and import the folder into Spring Tool Suite to run the spring boot application.

The python project needs to be run before the spring boot application is run.